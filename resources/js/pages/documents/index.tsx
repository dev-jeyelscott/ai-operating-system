import { Head, Link } from '@inertiajs/react';
export default function DocumentsIndex({ project, documents, urls }: any) { return <><Head title={`${project.name} documents`} /><main className="space-y-6 p-6"><Link href={urls.project}>Back to project</Link><h1 className="text-2xl font-semibold">Document center</h1><div className="grid gap-4">{documents.map((document: any) => <article key={document.id} className="rounded-xl border p-4"><Link href={`${location.pathname}/${document.id}`} className="font-semibold">{document.title}</Link>{document.versions.map((version: any) => <p key={version.id} className="mt-2 text-sm text-muted-foreground">v{version.version} · {version.status} · {version.classification}</p>)}</article>)}</div></main></>; }
