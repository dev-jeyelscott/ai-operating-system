# AI Operating System v0.1.0-rc.1

## Release status

Blocked and not published. This document reserves the release-candidate record;
it is not a Git tag, GitHub release, or approval to promote code.

## Candidate commit

Not established. A candidate is selected only after release documentation is
committed, all candidate validation completes, and the human promotion review
approves the exact `develop` head SHA.

## MVP scope

The MVP provides deterministic three-layer workflow orchestration, durable
tenant-scoped state, approval gates, real Notion task publication, accessible
dashboard and office projections, auditability, and operational recovery.

## Real capabilities

Workflow state, PostgreSQL persistence, queues, audits, approvals, Notion
publication, recovery controls, and test infrastructure are real capabilities.

## Simulated capabilities

Repository writes, pull requests, CI evidence from simulation, merges, and
production deployments remain simulated. No simulated evidence is verified.

## Explicit exclusions

No real merge or product-initiated production deployment occurs. Real provider
execution requires a later threat-model review.

## Architecture summary

See the [system overview](../architecture/system-overview.md).

## User-visible changes

See the [user guide](../product/user-guide.md) for the selected candidate.

## Migration inventory

No destructive MVP release migration is authorized. The selected candidate must
list every migration and its explicit rollback or recovery procedure.

## Environment and configuration changes

Record only changes verified for the selected candidate. None are approved yet.

## Validation evidence

Validation evidence must be copied from the approved candidate Definition of
Done. AIOS-294 is the approval authority, and the manual `release-candidate`
GitHub Actions run is the candidate evidence source. It is currently blocked;
see [MVP Definition of Done](../evidence/mvp-definition-of-done.md).

## Security status

The historical security review is invalidated. A fresh review with named human
approvals is required for the selected candidate.

## Accessibility status

Candidate accessibility tests and non-author user-guide verification are not
yet recorded.

## Known risks and operational constraints

Current blockers are incomplete candidate validation, missing independent QA,
Security Reviewer, and Release Manager approvals, absent user-guide/tabletop evidence, and unreconciled
promotion branches. A clean isolated full Laravel suite passed on 2026-08-03,
but it is not release evidence until tied to the selected commit. Follow the
[operations guide](../operations/operations-guide.md) and
[runbooks](../runbooks/README.md).

## Upgrade procedure

Use controlled expand-and-contract migration procedures only after the
candidate passes validation.

## Rollback procedure

Application rollback, migration recovery, data restore, object-version restore,
queue reconciliation, and projection rebuild are separate decisions.

## Backup and restore references

[backup and disaster recovery](../operations/backup-restore-disaster-recovery.md)
is the authoritative recovery process.

## Documentation index

Start at the [documentation index](../README.md) for product, architecture,
operations, security, testing, runbooks, evidence, and release records.

## Contributors and approvers

AIOS-294 is the role-level approval record. John Leward Escote
(`@dev-jeyelscott`) is Product Owner, System Architect, and Operations Owner.
The QA Owner, Security Reviewer, and Release Manager must be independent
reviewers. Until they are assigned and approve the same SHA, the disposition is
**Blocked awaiting independent reviewers**.
